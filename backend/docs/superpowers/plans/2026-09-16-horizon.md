# Horizon — implementation plan

Spec: `docs/superpowers/specs/2026-09-16-horizon-design.md`. Branch
`feature/horizon`, one commit per task, PR into `develop`.

- [ ] **Task 1: package + config.** `horizon:install`, supervisors per
  environment, `retry_after` 150, `horizon:publish` in `post-update-cmd`,
  committed assets. Test: config lists every dedicated queue per environment.
- [ ] **Task 2: gate + signed access.** `HorizonServiceProvider::auth`,
  `CreateHorizonAccessController` (API, `developer-access`),
  `HorizonAccessController` (web, `signed:relative`), `HORIZON_URL` config.
  Tests: gate matrix, access endpoint, signed redirect, unsigned 403.
- [ ] **Task 3: processes + scheduler.** `queue-workers.json` to three entries,
  `horizon:snapshot` schedule. Test: convention guard on the JSON.
- [ ] **Task 4: nginx + compose + env templates.** Second server block, port
  publish in the three compose files, env templates, `horizon:terminate`
  guidance in `CLAUDE.md`, deploy README prerequisite, `TODO.md` entry.
- [ ] **Task 5: frontend.** `openHorizon` action + test, "Open Horizon" button
  on System Controls.
- [ ] **Task 6: verify.** Backend suite + Pint; frontend tsc, lint, tests;
  container smoke: Horizon boots against the external Redis and the dashboard
  answers on the Horizon port.
