# docs/qa/

Manual end-to-end test plans, written for testers who are not engineers.

`YYYY-MM-DD-HHMM-qa-<scope>.md`, headed:

```markdown
# QA — <scope>

**Baseline:** develop @ <commit>. Regenerate this plan when endpoints change.

## Roles
| Role | How to get one |

## Test points
Numbered per area. Mark negative cases `NEG` and anything that needs a
developer to set up `🛠 needs dev`.
```

See `2026-09-15-0900-qa-example.md` for a filled-in example.
